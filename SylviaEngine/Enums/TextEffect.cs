namespace SylviaEngine.Enums;

public enum TextEffect
{
    None,
    Wave,      // Sine wave (smooth)
    Jitter,    // Random per-character
    Shake,      // Whole text shakes
    RainbowText,
    RainbowTextWave,
    TypeWriter
}